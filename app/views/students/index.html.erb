<div>
  <% @students.each do |student| %>
    <div><%= student.first_name + " " + student.last_name %></div>
    
  <% end %>
</div>

<div><%= link_to "student/:id", "/student/#{student.id}" %></div>


  <div><%= link_to post.title, post_path(post) %></div>
